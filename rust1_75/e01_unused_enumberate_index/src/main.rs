// 48. unused_enumerate_index

fn main() {
    // let v = vec![1, 2, 3, 4];
    // for (_, x) in v.iter().enumerate() {
    //     println!("{x}");
    // }
    let v = vec![1, 2, 3, 4];
    for x in v.iter() {
        println!("{x}");
    }
}
